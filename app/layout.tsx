import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'
import { Toaster } from '@/components/ui/toaster'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'EduConnect - Educational Management Platform',
  description: 'Streamline Education. Empower Teachers. Connect Communities.',
  keywords: ['education', 'management', 'teachers', 'students', 'school'],
  authors: [{ name: 'EduConnect Team' }],
  creator: 'EduConnect Team',
  publisher: 'EduConnect',
  icons: {
    icon: '/favicon.ico',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  )
}